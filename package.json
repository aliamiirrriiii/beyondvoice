{
  "name": "voiceai",
  "version": "0.1.0",
  "private": true,
  "description": "Lightweight voice calling app optimized for weak internet links.",
  "dependencies": {
    "redis": "^4.7.0",
    "ws": "^8.18.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build:neural-relay": "./scripts/build-neural-relay.sh",
    "build:neural-relay:native": "./scripts/build-neural-relay.sh --mode native-opus",
    "test": "node tests/extreme-stack.test.js && node tests/app-config.test.js && node tests/index-page.test.js && node tests/audio-path-review.test.js && node tests/neural-relay.test.js && node tests/media-relay.test.js && node tests/group-call.test.js && ./scripts/build-neural-relay.sh --mode native-opus && node tests/media-relay.e2e.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
